<?xml version="1.0" encoding="UTF-8" ?>
<!--Ant buildfile generated by Oracle JDeveloper-->
<!--Generated 26-sep-2019 11:32:22-->
<project xmlns="antlib:org.apache.tools.ant" name="SOALogging" default="all" basedir=".">
  <property file="build.properties"/>
  <path id="library.Commons.Logging.1.0.4">
    <pathelement location="${oracle.commons}/modules/org.apache.commons.logging_1.2.jar"/>
  </path>
  <path id="classpath">
    <path refid="library.Commons.Logging.1.0.4"/>
  </path>
  <target name="init">
    <tstamp/>
    <mkdir dir="${output.dir}"/>
  </target>
  <target name="all" description="Build the project" depends="compile,copy,create-jar"/>
  <target name="clean" description="Clean the project">
    <delete includeemptydirs="true" quiet="true">
      <fileset dir="${output.dir}" includes="**/*"/>
    </delete>
  </target>
  <!-- Build Project -->
  <target name="compile" description="Compile Java source files" depends="init">
    <javac destdir="${output.dir}" classpathref="classpath" debug="${javac.debug}" nowarn="${javac.nowarn}"
           deprecation="${javac.deprecation}" encoding="Cp1252" source="1.8" target="1.8">
      <src path="src"/>
    </javac>
  </target>
  <target name="copy" description="Copy files to output directory" depends="init">
    <patternset id="copy.patterns">
      <include name="**/*.cpx"/>
      <include name="**/*.dcx"/>
      <include name="**/*.ejx"/>
      <include name="**/*.gif"/>
      <include name="**/*.ini"/>
      <include name="**/*.jpeg"/>
      <include name="**/*.jpg"/>
      <include name="**/*.png"/>
      <include name="**/*.properties"/>
      <include name="**/*.sva"/>
      <include name="**/*.tag"/>
      <include name="**/*.tld"/>
      <include name="**/*.wsdl"/>
      <include name="**/*.xcfg"/>
      <include name="**/*.xlf"/>
      <include name="**/*.xml"/>
      <include name="**/*.xsd"/>
      <include name="**/*.xsl"/>
      <exclude name="build.xml"/>
    </patternset>
    <copy todir="${output.dir}">
      <fileset dir="src">
        <patternset refid="copy.patterns"/>
      </fileset>
      <fileset dir=".">
        <patternset refid="copy.patterns"/>
      </fileset>
    </copy>
  </target>
      <!-- create Jar -->
    <target name="create-jar" description="Create Jar file" depends="compile">
        <property name="build.jar.file" value="${build.jar.home}/${build.jar.name}"/>
        <echo message="create-jar ${ant.project.name}: ${ant.file}"></echo>
        <echo message="Create ${build.jar.file}"></echo>
        <jar destfile="${build.jar.file}" basedir="${build.classes.home}">
            <manifest>
                <attribute name="Main-Class" value="${build.jar.mainclass}"/>
            </manifest>
        </jar>
        <echo message="Copy ${build.jar.file} to ${dist.home}"></echo>
        <move todir="${dist.home}" overwrite="true">
            <fileset file="${build.jar.file}"/>
        </move>
    </target>
</project>
